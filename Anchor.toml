[toolchain]
anchor_version = "0.31.1"

[features]
resolution = true
skip-lint = false

# Substitua o ID abaixo pelo valor de `anchor keys list` apos o primeiro build.
# Ele precisa ser IDENTICO ao declare_id!() em programs/awakefl/src/lib.rs.
# `anchor keys sync` faz os dois de uma vez.
[programs.devnet]
awakefl = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[programs.localnet]
awakefl = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
