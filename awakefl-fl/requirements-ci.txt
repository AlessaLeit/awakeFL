# ---------------------------------------------------------------------------
# AwakeFL - versoes travadas para o CI
# ---------------------------------------------------------------------------
# O requirements.txt usa `>=` de proposito: quem instala na propria maquina
# precisa de folga para resolver com o que ja tem (principalmente torch no
# Windows). O CI nao tem esse problema e tem o problema oposto — um resultado
# que muda porque uma dependencia subiu de versao sozinha nao e reproduzivel,
# e reprodutibilidade e requisito de projeto de pesquisa.
#
# Este arquivo trava as versoes exatas. Para atualizar, suba os numeros aqui
# num commit proprio, com o motivo: assim da para amarrar qualquer mudanca de
# numero do experimento a uma mudanca de dependencia.
#
# torch e torchvision NAO estao aqui: o CI os instala do indice de CPU
# (download.pytorch.org), com as mesmas versoes travadas no workflow.
# ---------------------------------------------------------------------------

numpy==2.3.4
matplotlib==3.10.7
PyYAML==6.0.3
pytest==7.4.0
flwr==1.34.0
